# NexusDB LightRAG client (rag/ directory)
#
# These deps are consumed by ingest.py / query.py / watcher.py. The LightRAG
# server itself (lightrag-server/) has its own deps installed via
# `uv pip install "lightrag-hku[api]"` and lives in the project .venv.
#
# We deliberately keep this list minimal: the heavy lifting (embeddings,
# vector storage, graph extraction, reranker) happens inside the LightRAG
# server, not in this client.

httpx>=0.27          # REST client for /documents, /query, /health
watchdog>=3.0        # filesystem events for the watcher daemon
