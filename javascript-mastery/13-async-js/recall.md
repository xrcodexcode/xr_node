# Level 13 Recall

1. What is the difference between the Call Stack and the Callback Queue?
2. Which has higher priority: the Microtask Queue or the Macrotask Queue? Give an example of a task in each.
3. What are the three states of a Promise?
4. How is `Promise.all` different from `Promise.allSettled`?
5. Why does using `async/await` inside a `forEach` loop not work as expected? What should you use instead?
6. How do you catch an error in an `async` function?
7. What is "Callback Hell" and how do Promises solve it?
