# Active Recall: Error Handling and Debugging

1. What is the difference between a `ReferenceError` and a `TypeError`?
2. What are the three blocks used in error handling, and in what order do they execute?
3. How do you create a custom error class?
4. What happens to a Promise if an error is thrown inside an `async` function?
5. How can you measure how long a specific function takes to run using the `console` object?
6. When reading a stack trace, is the most recent function call at the top or the bottom?
7. What is an error boundary (conceptually)?
8. Why shouldn't you write an empty `catch (e) {}` block?
