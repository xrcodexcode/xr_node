# Level 18 Recall

1. What is a closure?
2. Explain what Currying is and why it might be useful.
3. What is the difference between `debounce` and `throttle`?
4. When should you use a `Map` instead of a plain JavaScript object?
5. When should you use a `Set` instead of an Array?
6. What is the specific benefit of a `WeakMap`?
7. What does the `yield` keyword do inside a generator function?
8. How does a Proxy differ from defining getters and setters directly on an object?
9. What is a pure function?
10. What is function composition?
