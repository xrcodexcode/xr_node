# Recall: The `this` Keyword

1. What does `this` refer to inside a regular function in strict mode?
2. How does an arrow function determine its `this` value?
3. What is the difference between `call` and `apply`?
4. Does `bind` execute the function immediately?
5. Why does `const { method } = myObject; method();` often cause errors?
6. Can you re-bind a function that has already been bound with `.bind()`?

*(Check your answers in the answer key or revise notes.md)*
