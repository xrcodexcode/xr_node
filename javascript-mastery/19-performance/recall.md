# Level 19 — Active Recall

1. What does O(n²) mean and when does it typically occur?
2. Why is `document.createDocumentFragment()` useful for performance?
3. How does Memoization work in JavaScript?
4. What is the difference between `performance.now()` and `Date.now()`?
5. Why is `.innerHTML +=` inside a loop a bad idea for performance?
6. Explain the concept of Event Delegation.
7. What is a reflow (or layout) in the browser?
8. How can a `Set` or `Map` help optimize a nested loop?
