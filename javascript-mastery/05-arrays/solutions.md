# Level 5 — Arrays Solutions

## Exercises Answers
### 🟢 Basic
1. `const fruits = ['Apple', 'Mango', 'Banana'];`
2. `arr[1]`
...

### 🟡 Intermediate
16. `nums.map(n => n * 2);`
...

## Worksheet Solutions
### Part A
1. `4` (Because arrays are reference types)
2. `[10, 2, 30]` (Default sort is alphabetical as strings!)
3. `false` (Different memory locations)

### Part D
1. Arrow function with curly braces needs a `return` keyword. `filter(n => { return n % 2 === 0 })` or `filter(n => n % 2 === 0)`.
