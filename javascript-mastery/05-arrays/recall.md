# Level 5 — Arrays Active Recall

1. What is the difference between `slice()` and `splice()`?
2. Does `map()` mutate the original array?
3. How does the default `sort()` behavior work on arrays of numbers?
4. What is returned by `forEach()`?
5. How can you empty an existing array?
6. What is the difference between `find()` and `filter()`?
7. How does the accumulator in `reduce()` work?
8. What does `Array.from()` do?
9. Explain the output of `[1, 2] === [1, 2]`.
10. How can you remove duplicates from an array?
11. What is the spread operator `...` used for with arrays?
12. What does `some()` return?
13. What is `.at(-1)` used for?
14. How do you flatten an array `[[1], [2]]`?
15. What happens if you modify an element inside an array copied with the spread operator (assuming shallow copy)?
