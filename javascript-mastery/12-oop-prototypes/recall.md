# Recall: Prototypes and OOP

1. What happens sequentially when you use the `new` keyword?
2. What is the difference between `__proto__` and `prototype`?
3. Why must you call `super()` before using `this` in a derived class constructor?
4. How do you create a private method in an ES6 class?
5. When should you use composition over inheritance?

*(Check your answers in the answer key or revise notes.md)*
