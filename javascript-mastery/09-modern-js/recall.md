# Level 9: Recall

1. What is the Temporal Dead Zone (TDZ)?
2. How does `this` differ in an arrow function vs a regular function?
3. What is the difference between `||` and `??`?
4. How do you skip an element in array destructuring?
5. When must you use `super()` in a class?
6. What is the difference between `for...in` and `for...of`?
