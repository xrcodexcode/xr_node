# Level 10: Recall

1. What is the Temporal Dead Zone?
2. Explain the difference between Creation Phase and Execution Phase.
3. What is a Closure in your own words?
4. How does `var` behave in a `for` loop with `setTimeout`, and why does `let` fix it?
5. What is the scope chain?
6. Are function declarations hoisted differently than function expressions? Explain.
