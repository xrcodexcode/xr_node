# Level 14 Recall

1. Does `fetch()` reject its promise if the server returns a 404 status code? Why or why not?
2. What are the two steps required to extract JSON data from a `fetch` GET request?
3. What is the difference between PUT and PATCH?
4. What header must you set when sending JSON data in a POST request?
5. How do you cancel an ongoing `fetch` request?
6. Name three 4xx status codes and what they mean.
7. Why must we use `JSON.stringify()` when sending an object in a POST body?
