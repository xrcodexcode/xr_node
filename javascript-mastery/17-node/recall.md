# Level 17 Recall

1. What is the fundamental difference between the Node.js runtime and the Browser runtime?
2. What does `__dirname` represent in a Node.js script?
3. How do you import a built-in module in CommonJS?
4. What is the difference between `fs.readFile` and `fs.readFileSync`?
5. What are the 3 main properties of `package.json`?
6. How does `npm install --save-dev` differ from `npm install`?
7. How do you access environment variables in Node.js?
8. What is the purpose of `res.writeHead` in the `http` module?
9. Explain the Node.js Event Loop in one sentence.
10. What is a Stream in Node.js and when would you use it instead of reading a whole file?
