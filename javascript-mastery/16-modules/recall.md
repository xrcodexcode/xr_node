# Active Recall: Modules and Code Organization

1. What is the difference between a default export and a named export?
2. Why do you need `./` when importing local files in ES Modules?
3. What does `<script type="module">` do differently from a standard `<script>` tag?
4. What is a "barrel file"?
5. What is circular dependency and why is it dangerous?
6. How do you import all named exports from a file into a single object? (e.g., `import * as ...`)
7. What is the syntax for dynamically importing a module?
