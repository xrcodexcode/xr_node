# Level 7: DOM - Active Recall

1. What is the difference between `NodeList` and `HTMLCollection`? Which one does `querySelectorAll` return?
2. Why is `textContent` generally preferred over `innerHTML`?
3. What is the difference between `appendChild` and `append`?
4. How do you select an element by a custom `data-*` attribute using `querySelector`?
5. How can you traverse from a child element to its parent?
6. What happens if you use `document.getElementById` and the ID doesn't exist?
7. Explain what `classList.toggle('active')` does.
8. How do you convert an `HTMLCollection` into a standard JavaScript array?
9. What is the difference between `nextSibling` and `nextElementSibling`?
10. If you do `element.innerHTML += '<p>new</p>'`, what happens to the existing DOM elements inside that element?
