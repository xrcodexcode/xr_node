import { useEffect, useState } from 'react'
import { Link, NavLink } from 'react-router-dom'
import { FiCommand, FiMenu, FiX } from 'react-icons/fi'
import { useSearchStore } from '../../store/searchStore'
const links = [['Issues','/issues'],['Articles','/articles'],['Topics','/categories'],['About','/about']]
export function Navbar() { const [open,setOpen]=useState(false); const [scrolled,setScrolled]=useState(false); const setSearchOpen=useSearchStore(s=>s.setOpen)
useEffect(()=>{const handler=()=>setScrolled(window.scrollY>12); handler(); addEventListener('scroll',handler,{passive:true}); return()=>removeEventListener('scroll',handler)},[])
return <header className={`fixed inset-x-0 top-0 z-40 transition-[background-color,border-color,backdrop-filter] duration-300 ${scrolled?'border-b border-white/[.07] bg-ink/80 backdrop-blur-xl':''}`}><nav className="container-site flex h-20 items-center justify-between" aria-label="Main navigation"><Link to="/" className="rounded-sm font-display text-xl font-extrabold tracking-[-.08em] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300">Nexus<span className="text-cyan-300">DB</span></Link><div className="hidden items-center gap-7 md:flex">{links.map(([label,to])=><NavLink key={to} to={to} className="nav-link">{label}</NavLink>)}</div><div className="flex items-center gap-3"><button onClick={()=>setSearchOpen(true)} className="hidden items-center gap-2 rounded-full border border-white/10 px-3 py-2 text-xs text-zinc-400 transition hover:border-white/30 hover:text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300 sm:flex" aria-label="Search"><FiCommand /> Search <kbd className="rounded border border-white/10 px-1 text-[9px]">K</kbd></button><Link to="/#subscribe" className="button-primary hidden !px-4 !py-2.5 sm:inline-flex">Subscribe</Link><button onClick={()=>setOpen(!open)} className="grid h-10 w-10 place-items-center rounded-full border border-white/10 transition hover:bg-white/[.06] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300 md:hidden" aria-label="Toggle menu" aria-expanded={open}>{open?<FiX/>:<FiMenu/>}</button></div></nav>{open&&<div className="container-site border-t border-white/10 bg-ink/95 py-5 backdrop-blur-xl md:hidden"><div className="flex flex-col gap-5">{links.map(([label,to])=><NavLink onClick={()=>setOpen(false)} key={to} to={to} className="rounded-sm text-lg text-zinc-300 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300">{label}</NavLink>)}<Link onClick={()=>setOpen(false)} to="/#subscribe" className="button-primary">Subscribe to NexusDB</Link></div></div>}</header> }
