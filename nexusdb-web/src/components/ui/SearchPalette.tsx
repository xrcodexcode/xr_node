import { useEffect, useRef } from 'react'; import { useNavigate } from 'react-router-dom'; import { FiSearch, FiX } from 'react-icons/fi'
import { useSearchStore } from '../../store/searchStore'
const results=[['Latest issue','/issues'],['Research archive','/archive'],['AI categories','/categories'],['About NexusDB','/about']]
export function SearchPalette(){const {isOpen,setOpen}=useSearchStore(); const nav=useNavigate(); const closeRef=useRef<HTMLButtonElement>(null); useEffect(()=>{const onKey=(e:KeyboardEvent)=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setOpen(true)} if(e.key==='Escape')setOpen(false)};addEventListener('keydown',onKey);return()=>removeEventListener('keydown',onKey)},[setOpen]); useEffect(()=>{if(isOpen)closeRef.current?.focus()},[isOpen]);if(!isOpen)return null;return <div role="dialog" aria-modal="true" aria-label="Search NexusDB" className="fixed inset-0 z-[60] grid place-items-start bg-black/60 p-4 pt-[max(5rem,18vh)] backdrop-blur-sm" onMouseDown={()=>setOpen(false)}><div className="w-full max-w-xl overflow-hidden rounded-2xl border border-white/15 bg-[#151515] shadow-2xl" onMouseDown={e=>e.stopPropagation()}><div className="flex items-center gap-3 border-b border-white/10 px-5"><FiSearch className="text-cyan-300"/><input autoFocus aria-label="Search the knowledge base" placeholder="Search the knowledge base…" className="h-14 min-w-0 flex-1 bg-transparent text-sm outline-none placeholder:text-zinc-600"/><button ref={closeRef} onClick={()=>setOpen(false)} className="rounded-md p-2 transition hover:bg-white/10 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300" aria-label="Close search"><FiX/></button></div><div className="p-2">{results.map(([label,to])=><button key={to} onClick={()=>{nav(to);setOpen(false)}} className="flex w-full items-center justify-between rounded-xl px-4 py-3 text-left text-sm text-zinc-300 transition hover:bg-white/[.06] hover:text-white focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-300"><span>{label}</span><span className="text-zinc-600">Go to →</span></button>)}</div></div></div>}
