import { Canvas, useFrame } from '@react-three/fiber'; import { Points, PointMaterial } from '@react-three/drei'; import { useMemo, useRef } from 'react'; import * as THREE from 'three'
function Field(){const ref=useRef<THREE.Points>(null);const points=useMemo(()=>{const p=new Float32Array(520*3);for(let i=0;i<p.length;i++)p[i]=(Math.random()-.5)*12;return p},[]);useFrame((state,dt)=>{if(ref.current){ref.current.rotation.y+=dt*.018;ref.current.rotation.x=Math.sin(state.clock.elapsedTime*.07)*.05}});return <Points ref={ref} positions={points} stride={3} frustumCulled><PointMaterial transparent color="#74d8ff" size={.02} sizeAttenuation depthWrite={false} opacity={.62}/></Points>}
export function ParticleField(){return <div className="pointer-events-none absolute inset-0 opacity-60 motion-reduce:hidden"><Canvas dpr={[1,1.25]} camera={{position:[0,0,4.5],fov:55}} gl={{antialias:false,alpha:true,powerPreference:'high-performance'}}><Field/></Canvas></div>}
